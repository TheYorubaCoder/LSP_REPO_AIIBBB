The PriceCalculator class has two main design problems. If the pricing logic for any customer type becomes more complex, all of that logic piles up inside a single method, making the code harder to read and maintain. Additionally, adding new customer tiers requires modifying the existing method directly, which increases the risk of breaking behavior that already works.